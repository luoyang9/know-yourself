<div id="fb-root"></div>

<!-- Navigation -->
<nav class="navbar navbar-custom navbar-fixed-top" role="navigation">
    <div class="container">
        <div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-main-collapse">Menu<i class="fa fa-bars"></i></button>		
			<a class="navbar-brand page-scroll" href="#page-top">		
			<i class="fa fa-play-circle"></i> {{title}}		
			</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse navbar-right navbar-main-collapse">
            <ul class="nav navbar-nav">
                <!-- Hidden li included to remove active class from about link when scrolled up past about section -->
                <li class="hidden">
                    <a href="#page-top"></a>
                </li>
                <li>
              		<a class="page-scroll" ng-show="loggedIn" ng-click="main.logoutFacebook()">Welcome, {[{user.name}]}</a>
                </li>
                <li ng-show="loggedIn">
              		<button ng-click="main.logoutFacebook()" class="btn btn-default">Log Out</button>
                </li>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container -->
</nav>

<!-- Intro Header -->
<header id="page-top" class="intro">
    <div class="intro-body">
        <div class="container">
            <div class="row">
                <div class="col-md-8 col-md-offset-2">
                    <h1 class="brand-heading">{{title}}</h1>
                    <p class="intro-text">What does your facebook profile show about you?</p>
                    <button ng-hide="loggedIn" ng-click="main.loginFacebook()" class="btn btn-default">Log In</button>

                    <a class="page-scroll" href="#info"><button ng-show="loggedIn" ng-click="main.generate()" class="btn btn-default">Generate</button></a>
                </div>
            </div>
        </div>
    </div>
</header>

<section id="info" class="container content-section text-center">
	<div class="row">
		<div class="col-sm-12" style="margin-bottom: 30px;">
            <h3 class='center'>Average Positivity of Your Posts: {[{positivity.average}]}%</h3>
			<p style="margin-top: 30px;">Most Positive Post: <strong>{[{positivity.most}]}</strong></p>
			<p>Least Positive Post: <strong>{[{positivity.least}]}</strong></p>
		</div>
	</div>
		<hr />
	<div class="row" style="margin-top: 90px;">
		<div class="col-sm-12" style="margin-bottom: 30px;">
			<p style="margin-top: 30px;">Angriest Post: <strong>{[{emotion.anger}]}</strong></p>
			<p style="margin-top: 30px;">Most Surprised Post: <strong>{[{emotion.surprise}]}</strong></p>
			<p style="margin-top: 30px;">Saddest Post: <strong>{[{emotion.sad}]}</strong></p>
			<p style="margin-top: 30px;">Most Fearful Post: <strong>{[{emotion.fear}]}</strong></p>
			<p style="margin-top: 30px;">Most Joyful Post: <strong>{[{emotion.joy}]}</strong></p>
		</div>
	</div>
		<hr />
	<div class="row" style="margin-top: 90px;">
		<div class="col-sm-6">

            <h3 class='center'>Interests Based on Posts/Likes</h3>

            <div class="center" id="InterestsChart"></div>

            <br>
		</div>
        <div class="col-sm-6">
            <h3 class='center'>Popularity of Profile Pic</h3>

            <div class='center' id="chartdiv"></div>
        </div>
	</div>
</section>

<section class="container content-section text-center">
	<h3>What do your photos show about your emotions?</h3>
    <button class="btn btn-default" ng-click="main.getPhotos()">Get Photos</button>
	<div class="row" style="margin-top: 30px;">
		<div class="col-lg-2">
			<h2>Happy</h2>
			<div class="photo" ng-repeat="photo in photoEmotions | filter:{matchedEmotion: 'Happy'}">
				<a href="{[{photo.link}]}" target="_blank"><img ng-src="{[{photo.url}]}" /></a>
			</div>
		</div>
		<div class="col-lg-2">
			<h2>Surprise</h2>
			<div class="photo" ng-repeat="photo in photoEmotions | filter:{matchedEmotion: 'Surprise'}">
				<a href="{[{photo.link}]}" target="_blank"><img ng-src="{[{photo.url}]}" /></a>
			</div>
		</div>
		<div class="col-lg-2">
			<h2>Sad</h2>
			<div class="photo" ng-repeat="photo in photoEmotions | filter:{matchedEmotion: 'Sad'}">
				<a href="{[{photo.link}]}" target="_blank"><img ng-src="{[{photo.url}]}" /></a>
			</div>
		</div>
		<div class="col-lg-2">
			<h2>Fear</h2>
			<div class="photo" ng-repeat="photo in photoEmotions | filter:{matchedEmotion: 'Fear'}">
				<a href="{[{photo.link}]}" target="_blank"><img ng-src="{[{photo.url}]}" /></a>
			</div>
		</div>
		<div class="col-lg-2">
			<h2>Angry</h2>
			<div class="photo" ng-repeat="photo in photoEmotions | filter:{matchedEmotion: 'Angry'}">
				<a href="{[{photo.link}]}" target="_blank"><img ng-src="{[{photo.url}]}" /></a>
			</div>
		</div>
		<div class="col-lg-2">
			<h2>Neutral</h2>
			<div class="photo" ng-repeat="photo in photoEmotions | filter:{matchedEmotion: 'Neutral'}">
				<img ng-src="{[{photo.url}]}" />
			</div>
		</div>
	</div>
</section>

<!-- About Section -->
<section id="about" class="container content-section text-center">
    <div class="row">
        <div class="col-lg-8 col-lg-offset-2">
            <h2>About {{title}}</h2>
            <p>{{title}} is a hack created at Hack the 6ix Summmer 2016. It analyzes your Facebook profile and provides a report of information, such as your most talked about topic, your popularity, and the emotions behind your photos.</p>
        </div>
    </div>
</section>

<!-- Footer -->
<footer>
    <div class="container text-center">
        <p>Copyright &copy; Team AsiansFromUW 2016</p>
    </div>
</footer>



<!-- jQuery -->
<script src="javascripts/jquery.min.js"></script>

<!-- Plugin JavaScript -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-easing/1.3/jquery.easing.min.js"></script>

<!-- Bootstrap -->
<script src="/javascripts/bootstrap.min.js"></script>

<!-- Theme JavaScript -->
<script src="javascripts/grayscale.js"></script>